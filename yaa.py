"""This function will be the heart of the program. It will turn the doc the user enters into a set of terms and definitions"""
# whitepower
defSet ={a:b, c:d, e:f}

# interate through lines of text
# search through each line of text for tab ('    ') with if statement
# under the if function assign the string of text following the tab to value a
# search for colon with new if statement
# assign that string to b
# cont.



"""we have to find a way to icrement the position in the dictionary that we are assigning values to as they are being defined"""